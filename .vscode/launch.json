{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run in Docker",
      "type": "python",
      "request": "attach",
      "port": 5678,
      "host": "localhost",
      "django": true,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}
